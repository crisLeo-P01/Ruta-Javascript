// map()

let numbers = [2, 4, 6, 8, 10]
let mapNumber = numbers.map(num => num * num)

console.log(numbers);
console.log(mapNumber);

// forEach()

let colors = ['red', 'yellow', 'blue']
let itereitedColors = colors.forEach(color => console.log(color))

console.log(colors);
console.log(itereitedColors);
